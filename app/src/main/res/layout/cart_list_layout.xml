<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rl_cart_list"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_cart"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="50dp"></androidx.recyclerview.widget.RecyclerView>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_above="@+id/rv_cart"
        android:background="#ddd"
        android:gravity="center_vertical"
        android:paddingLeft="10dp"
        android:text="已选商品"></TextView>

    <TextView
        android:id="@+id/tv_clear"
        android:layout_width="70dp"
        android:layout_height="40dp"
        android:layout_above="@id/rv_cart"
        android:layout_alignParentRight="true"
        android:layout_marginRight="10dp"
        android:drawableLeft="@drawable/icon_clear"
        android:gravity="center_vertical"
        android:text="清空"></TextView>
</RelativeLayout>